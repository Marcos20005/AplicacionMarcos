import javax.swing.JFrame;

public class App {

    public static void main(String[] args) throws Exception {
      JFrame cuadrado = new JFrame();
        cuadrado.setTitle("Aplicacion hecha por Marcos");
        cuadrado.setSize(500, 500);
        cuadrado.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        cuadrado.setLocationRelativeTo(null);   
        cuadrado.setVisible(true);
        //Manuel Maricon

    }
}